<div class="px-4 sm:px-6 lg:px-8 py-8">
  <h1 class="text-2xl font-bold text-slate-900 mb-6">Search Results</h1>

  <% if @query.length < 2 %>
    <p class="text-sm text-slate-500">Enter at least 2 characters to search.</p>
  <% elsif @results.empty? %>
    <p class="text-sm text-slate-500">No results found for "<%= @query %>".</p>
  <% else %>
    <p class="text-sm text-slate-500 mb-4"><%= @results.size %> result(s) for "<%= @query %>"</p>
    <div class="bg-white ring-1 ring-slate-200 rounded-xl divide-y divide-slate-100">
      <% @results.each do |result| %>
        <%= link_to polymorphic_path(result[:record]), class: "block px-6 py-4 hover:bg-indigo-50" do %>
          <div class="flex items-center justify-between">
            <div>
              <span class="text-sm font-medium text-slate-900"><%= result[:name] %></span>
              <% if result[:type] == "Proposal" %>
                <span class="text-sm text-slate-500 ml-2">— <%= result[:company] %></span>
              <% end %>
            </div>
            <% type_colors = { "Prospect" => "bg-indigo-100 text-indigo-800", "Customer" => "bg-green-100 text-green-800", "Proposal" => "bg-purple-100 text-purple-800" } %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium <%= type_colors[result[:type]] %>">
              <%= result[:type] %>
            </span>
          </div>
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>
